# Personal Finance Manager

A simple web-based application to track daily expenses in an organized way.  
Built using Python (Flask) for the backend, MySQL for database storage, and HTML & CSS for the frontend.

## Features
- Add daily expenses
- Categorize expenses (Food, Travel, Shopping, etc.)
- View all expenses in a tabular format
- Secure data storage using MySQL

## Tech Stack
- Python (Flask)
- MySQL
- HTML
- CSS

## Project Structure
personal-finance-manager/
├── app.py
├── db.sql
├── requirements.txt
├── templates/
│ └── index.html
└── static/
└── style.css

## Setup
1. Clone the repository

git clone https://github.com/your-username/personal-finance-manager.git
cd personal-finance-manager
Install dependencies

pip install -r requirements.txt


Setup database

source db.sql;


Run the app

python app.py


Open http://127.0.0.1:5000/ in your browser.
